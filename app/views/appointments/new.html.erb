<%= simple_form_for [@doctor, @appointment] do |f| %>
  <%= f.input :date, as: :datetime, start_year: Date.today.year, end_year: Date.today.year + 1, start_hour: 8, end_hour: 17, minute_step: 15, order: [:day, :month, :year] %>
  <%= f.association :patient %>
  <%= f.submit %>
<% end %>
